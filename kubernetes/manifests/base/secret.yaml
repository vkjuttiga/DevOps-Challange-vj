# Secret Template - DO NOT COMMIT ACTUAL SECRETS
# Use kubectl create secret or external secrets manager

apiVersion: v1
kind: Secret
metadata:
  name: devops-demo-app-secrets
  labels:
    app: devops-demo-app
type: Opaque
stringData:
  # Example secrets - replace with actual values using:
  # kubectl create secret generic devops-demo-app-secrets \
  #   --from-literal=API_KEY=your-api-key \
  #   --from-literal=DB_PASSWORD=your-password
  EXAMPLE_SECRET: "placeholder-replace-me"
