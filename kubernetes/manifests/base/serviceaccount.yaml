apiVersion: v1
kind: ServiceAccount
metadata:
  name: devops-demo-app
  labels:
    app: devops-demo-app
  annotations:
    # IRSA annotation - uncomment and add IAM role ARN if using AWS services
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/devops-demo-app-role
